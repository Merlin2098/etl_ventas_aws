# Moved to tests/conftest.py so tests/e2e/ can share the aws_client/tf_outputs
# fixtures too. Kept as an empty module (pytest requires the file only if it
# declares fixtures/hooks — safe to leave present but inert).
